SHOW CON_NAME;
SELECT PDB_NAME, STATUS FROM DBA_PDBS;
SELECT NAME, OPEN_MODE FROM V$PDBS;


ALTER SESSION SET CONTAINER = CDB$ROOT;

ALTER PLUGGABLE DATABASE XEPDB1 CLOSE IMMEDIATE;

DROP PLUGGABLE DATABASE XEPDB1 INCLUDING DATAFILES;

CREATE PLUGGABLE DATABASE RESERVATION_PDB
ADMIN USER reservation_admin IDENTIFIED BY admin
ROLES = (DBA)
FILE_NAME_CONVERT = ('/opt/oracle/oradata/XE/pdbseed', '/opt/oracle/oradata/XE/RESERVATION_PDB');

ALTER PLUGGABLE DATABASE RESERVATION_PDB OPEN;

ALTER SESSION SET CONTAINER = RESERVATION_PDB;

CREATE TABLESPACE hd_user_ts
DATAFILE '/opt/oracle/oradata/XE/RESERVATION_PDB/hd_user_ts01.dbf'
SIZE 100M AUTOEXTEND ON
NEXT 10M MAXSIZE 1G;


CONNECT sys/password123@//localhost:1521/RESERVATION_PDB AS SYSDBA;

CREATE USER hd_user IDENTIFIED BY hotdesk
DEFAULT TABLESPACE hd_user_ts
QUOTA UNLIMITED ON hd_user_ts;
GRANT CREATE SESSION, RESOURCE TO hd_user;
GRANT CREATE PROCEDURE TO hd_user;
GRANT UNLIMITED TABLESPACE TO hd_user;

COMMIT;